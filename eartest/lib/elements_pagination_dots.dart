import 'package:flutter/material.dart';
import 'package:adobe_xd/pinned.dart';

class ElementsPaginationDots extends StatelessWidget {
  ElementsPaginationDots({
    Key? key,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Stack(
      children: <Widget>[
        Container(),
        Container(),
        Container(),
        Container(),
        Container(),
      ],
    );
  }
}
